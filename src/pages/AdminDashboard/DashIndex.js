import React from "react";

const DashIndex = () => {
   return (
      <div>
         <p className="flex lg:mt-20 mt-6 justify-center italic text-lg text-gray-600">
            If you're here, you suppose to be admin. Select a route from left
            slidebar to manage.
         </p>
      </div>
   );
};

export default DashIndex;
